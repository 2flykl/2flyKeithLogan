# Streams Sprite Engine V2.1

This replaces the placeholder Streams prototype with a sprite-connected engine.

Install by replacing the contents of `games/streams/` with this package.

Features: real character animation frames, music-object platforms, legacy pickups, attention artwork, layered water textures, gentle 15-second tutorial, increasing current, and a stage goal.
