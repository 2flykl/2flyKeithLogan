STREAMS STABLE FPS REBUILD

This build fixes a fatal startup reset bug (undefined dt), strips expensive frame-by-frame rendering, adds FPS telemetry, and launches via localhost to avoid file:// cache/origin oddities.

Run START_STREAMS_STABLE_FPS.bat
