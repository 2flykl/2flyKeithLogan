const root='../../';const app=document.querySelector('#app');try{const rooms=await fetch(`${root}assets/games/thru-the-fire/manifests/room-views.json`).then(r=>{if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()});app.innerHTML='<div class="warning"><strong>Quality note:</strong> these are lossless prototype crops. Final production rooms should be exported as eight individual 1536×768 images per room.</div>';for(const room of Object.values(rooms)){const s=document.createElement('section');s.className='room';s.innerHTML=`<h2>${room.displayName}</h2><div class="views"></div>`;const g=s.querySelector('.views');for(const view of room.views){const c=document.createElement('article');c.className='card';c.innerHTML=`<img src="${root}${view.path}" alt="${room.displayName}, ${view.degrees} degrees"><p>${view.degrees}° — ${view.name}</p>`;g.append(c)}app.append(s)}}catch(e){app.innerHTML=`<div class="warning">Could not load manifest: <code>${e.message}</code>. Use GitHub Pages or a local web server.</div>`}